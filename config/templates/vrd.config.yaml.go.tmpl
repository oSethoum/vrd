# the path where you'r veuerd schema is
input: "vrd/db.vuerd.json"

# where we output the files
output: "./output"

# ent specefic configuration
ent:
  package: "app"
  graphql: true
  auth: true
  privacy: true
  file_upload: true
  debug: true
  database: "sqlite3" # sqlite3 | postgres | mysql
